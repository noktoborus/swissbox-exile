Сборка сервера:
	cd server && make
сборка сервера с отладкой:
	cd server && make debug
запуск сервера:
	psql fepserver <sql/struct.sql
	cd server && ./bin/server
запуск тестов:
	cd server && make runtest

в server/additional_flags добавлять флаги для компилятора (разделять пробелами)
